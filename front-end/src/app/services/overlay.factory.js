(function() {
    'use strict';
  
    angular.module('app')
      .factory('Overlay', OverlayFactory);
  
    function OverlayFactory($q, config, Data, GeoUtils) {
        //this service will hold the overlays

        return {
            
        }
    }
  })();
  